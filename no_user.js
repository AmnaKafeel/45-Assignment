"use strict";
let userName = ['Admin', 'Nabeel', 'Kafeel', 'Farhaan'];
if (userName.length === 0) {
    console.log("We need to find some users!");
}
else {
    userName = [];
    console.log('All Users Name has been Removed. ' + userName.length);
}
